# Context

## Terms

- CLI event decoding: Translation of newline-delimited JSON replay input into
  core `Event` values, including event-shape validation, field parsing, default
  currency handling, and line/type diagnostic context.
